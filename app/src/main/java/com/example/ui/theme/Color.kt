package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Minimalist Modern Palette (OLED Black + Zinc Grayscale + Crisp Accents)
val DarkBackground = Color(0xFF09090B)       // True deep OLED background (Zinc-950)
val DarkSurface = Color(0xFF121215)          // Elevated neutral surface
val DarkSurfaceVariant = Color(0xFF18181B)   // Cards, grouped containers (Zinc-900)
val DarkSurfaceElevated = Color(0xFF202024)  // Floating pills, dialogs, sheets
val BorderGlass = Color(0xFF27272A)          // Hairline border (Zinc-800)
val BorderSubtle = Color(0x1AFFFFFF)         // 10% white frosted border

// Modern Brand & Accent Colors
val AccentPrimary = Color(0xFFFFFFFF)        // Pure white high-contrast primary
val AccentBlue = Color(0xFF3B82F6)           // Electric cobalt for subtle interactive accents
val CyanAccent = Color(0xFFF4F4F5)           // Replaced neon cyan with clean minimalist white/silver
val CyanPrimary = Color(0xFFE4E4E7)          // Clean secondary accent
val ElectricBlue = Color(0xFF3B82F6)         // Modern cobalt accent
val DeepNavy = Color(0xFF09090B)             // Deep contrast background

// Semantic Detection Colors (Refined & Restrained)
val HighConfidenceColor = Color(0xFF10B981)   // Emerald Green
val MediumConfidenceColor = Color(0xFF3B82F6) // Cobalt Blue
val LowConfidenceColor = Color(0xFFF59E0B)    // Warm Amber
val CriticalRed = Color(0xFFEF4444)           // Clean Coral Red

// Text Typography Colors
val TextWhite = Color(0xFFFAFAFA)            // Crisp primary text
val TextGray = Color(0xFFA1A1AA)             // Balanced secondary text (Zinc-400)
val TextMuted = Color(0xFF71717A)            // Muted tertiary text (Zinc-500)

// Glassmorphism overlays
val GlassDarkBg = Color(0xB3121215)          // 70% dark blur backdrop
val GlassPillBg = Color(0x9918181B)          // Translucent floating controls

// Light theme counterparts
val LightBackground = Color(0xFFFAFAFA)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFF4F4F5)
val LightTextPrimary = Color(0xFF09090B)
val LightTextSecondary = Color(0xFF71717A)
